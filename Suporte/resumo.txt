#Criar a imagem docker
docker build -t web_apache .

#subir uma imagem
docker run -p 80:80 web_apache

#listar imagens
docker image ls

#Subir uma imagem desacoplando do terminal
docker run -d -p 80:80 web_apache

#listar imagem
docker ps

#interromper imagem
docker stop "passar o contaner id"

#docker compose subir
docker-compose up db

#matar a rede do compose
docker-compose down

#docker compose subir, liberar terminal
docker-compose up -d db
obs.db bi nome do compose

#ver serviços docker compose
docker-compose ps

#ctrl+shift+p
#crtl+shift+f
#ctrl+enter

#python criar ambiente virtual
python -m venv .venv

#pacotes para usar sql no python
pip install SQLAlchemy
pip install psycopg2
pip install pandas
pip freeze > requirements.txt


#https://www.youtube.com/watch?v=RE31GWJGkwA&t=4s


docker logs passaridimagem
docker logs idimages -f
docker exec passaridimagem ls/
docker exec -it passaridimagem bash

#metabase





